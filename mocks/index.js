const products = require('./products.mock');
const reviews = require('./reviews.mock');
const categories = require('./categories.mock');
const shoppingCarts = require('./shopping.carts.mock');

module.exports = {
  products,
  reviews,
  categories,
  shoppingCarts
};
